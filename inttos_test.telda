include lib/io.telda, lib/convert.telda

start:
    push ~21
    # dirty to use the start as the string buffer, but these pushs are long enough
    push start
    call IntTos
    call Print

    halt

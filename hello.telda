hello:
    dat "Hello World\n", 0

start:
    load $sr, hello
loop:
    # Load the character at the current index and print it
    sld $ab
    # If it's a null byte, we jump to the end
    jez end
    # Otherwise print the character and loop
    int2
    jmp loop

end:
    halt
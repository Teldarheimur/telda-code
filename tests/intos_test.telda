.ref _print
.ref _uitos

.seg text
.entry
_start:
    ldi r6b, 21
    ; dirty to use the start as the string buffer, but these adds are long enough
    ldi r7, _start
    call _uitos
    ldi r6, _start
    call _print

    halt
